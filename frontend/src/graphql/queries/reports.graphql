query GetAllClientsAndProjectsWithTasks {
    clients {
        id,
        name,
        projects {
            id, 
            name,
            tasks {
                id, 
                name
            }
        }
    }
}