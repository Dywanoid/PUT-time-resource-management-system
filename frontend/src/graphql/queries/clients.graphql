query GetAllClients {
        clients(limit: 100) {
            id,
            name,
            taxId,
            currency,
            streetWithNumber,
            zipCode,
            city
        }
    }